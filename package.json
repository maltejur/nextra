{
  "name": "nextra-monorepo-patch-maltejur",
  "private": true,
  "workspaces": [
    "packages/nextra",
    "packages/nextra-theme-docs",
    "packages/nextra-theme-blog",
    "examples/docs",
    "examples/blog"
  ],
  "scripts": {
    "build:core": "yarn workspace nextra-patch-maltejur run build",
    "build:theme-docs": "yarn workspace nextra-theme-docs-patch-maltejur run build",
    "build:theme-blog": "yarn workspace nextra-theme-blog-patch-maltejur run build"
  },
  "devDependencies": {
    "prettier": "^2.2.1"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "trailingComma": "none",
    "arrowParens": "avoid"
  }
}
